{
  "name": "tube-to-streaming",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "publish": "cd webapp && npm run build && cd .. && gcloud --quiet app deploy",
    "deploy": "gcloud --quiet app deploy",
    "browse": "gcloud app browse"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "fs": "^0.0.1-security",
    "http": "^0.0.1-security",
    "ytdl-core": "^4.8.3"
  }
}
